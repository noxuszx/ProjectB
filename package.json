{
  "scripts": {
    "prettier": "prettier",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:lua": "prettier --write **/*.lua"
  },
  "devDependencies": {
    "@prettier/plugin-lua": "^0.0.3",
    "prettier": "3.6.2"
  }
}
